import { CalendarDate } from "./calendar-date.interface";

/**
 * Интерфейс для построения таблицы модального окна
 */
export interface CalendarDates {
  /** Список ячеек для таблицы модального окна */
  dates: CalendarDate[];
}